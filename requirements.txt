# Core
pandas>=2.2
numpy>=1.26
openpyxl>=3.1          # read NHS .xlsx publications
pyarrow>=15.0          # parquet for interim/processed data
requests>=2.31         # download raw files

# Validation / profiling
pandera>=0.20          # schema + statistical validation (later phases)
# NOTE: version specifiers here are minimum bounds, not pins. No lockfile yet
# (see docs/assumptions.md "Reproducibility gaps"). Keep this file aligned with
# [project.dependencies] in pyproject.toml.

# Notebooks / viz
jupyterlab>=4.0
matplotlib>=3.8

# Dev / test
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
